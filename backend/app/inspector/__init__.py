"""Inspector portal backend — assignment, assessment forms, IndexedDB sync.

Sprint 6 scope: inspector onboarding, assignment view, property assessment form,
area-infrastructure scoring (stored against GPS coordinates, shared across
listings in the same geographic cell), photo/video upload with GPS+timestamp,
report submission. Every submission routes through the admin review queue —
inspectors cannot commit directly.
"""
