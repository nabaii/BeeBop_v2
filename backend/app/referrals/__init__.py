"""Referral programme — code issuance, attribution, earnings, payouts.

Pure maths live in `rewards.py`; code issuance in `codes.py`. Service wiring,
the clearing sweeper, the dashboard, and the admin view land in later sprints.
"""
