"""Short-let in-booking group chat — WebSocket transport, database-persisted
messages, content filtering, moderator presence.

Sprint 12 scope: chat thread auto-creation on booking confirmation (seeker +
host + BeeBop moderator), WebSocket real-time delivery, regex content filter
(NG phone formats, 10-digit account strings, Nigerian bank names, off-platform
payment phrases), on-platform action shortcuts (extend booking, early check-in,
report issue). Chat is short-let only — blocked at API level for other types.
"""
