"""Authentication and session management.

Sprint 1 scope: email OTP (Resend), WhatsApp OTP (Business API), JWT issuance
with refresh rotation, role-based route guards (seeker, landlord, inspector,
agent, admin).
"""
